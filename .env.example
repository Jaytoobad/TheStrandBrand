# Safe to expose in the frontend build — these are public identifiers, not secrets.
VITE_SUPABASE_URL=https://orsipouxwpxowsgzgjxf.supabase.co 
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_-WPSqU40OYxHij2gcQMfdQ_mw9-zDOS

# Paystack PUBLIC key only (starts with pk_). Never put the secret key (sk_) here.
VITE_PAYSTACK_PUBLIC_KEY=pk_test_59670eef72b7a4dc0e3dc532ae7f70f8a6c9b1ff

# --------------------------------------------------------------------------
# The following are SERVER-SIDE ONLY. They do NOT go in this file.
# They are set as Supabase Edge Function secrets instead, via:
#   supabase secrets set PAYSTACK_SECRET_KEY=sk_test_xxxx
#   supabase secrets set SUPABASE_SERVICE_ROLE_KEY=xxxx
# Never commit them, never reference them from src/, never prefix with VITE_.
# --------------------------------------------------------------------------
